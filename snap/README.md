# SourceGate v2.1 runtime snapshots — FINAL

These are the curated load-bearing screenshots for the frozen v2.1 contract `0x90F760d90642325777a97Fb9640c5E3fB0d8c2A6`. No v2.0 screenshot is valid evidence for this source.

- `00`–`02`: real GenVM Direct Mode, release/SHA verification, fresh StudioNet v2.1 deployment.
- `03`: wrong-role Author attestation refused while provenance state stays unchanged.
- `04`: first successful natural semantic judgment seals the active basis.
- `05`: two positive pairs plus one unjudged pair still blocks reuse.
- `06`: same Author freeze call succeeds only after the complete all-independent matrix.
- `07`: typed reuse creates deterministic upstream `Reuse = 1`.
- `08`: natural `DERIVATIVE_SOURCE_CLUSTER` sets permanent history block.
- `09`: Reviewer cannot remove the judged derivative source after seeing the verdict.
- `10`: Author freeze is refused with exact `permanent derivative-history block` reason.

Exact full transaction hashes, Explorer links, and observed post-state are recorded in [`../RUNTIME_EVIDENCE.md`](../RUNTIME_EVIDENCE.md).
